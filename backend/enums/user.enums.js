const ROLES = Object.freeze({
    ADMIN: "admin",
    SALES: "sales",
    INVENTORY: "inventory",
    ACCOUNTING: "accounting",
    PLANNER: "planner",
    PRODUCTION:"production",
});

module.exports = { ROLES };
